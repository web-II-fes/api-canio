export const hosts = {
    mongoDb_main: {
        host: 'mongodb://localhost/prueba',
        options: {
            reconnectTries: Number.MAX_VALUE
        }
    }
};

export const key = {
    secret : 'ifes-secret-key'
};